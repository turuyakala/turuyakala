/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // İstersen:
  // experimental: { serverActions: { allowedOrigins: ['turuyakala.com','www.turuyakala.com'] } }
}

module.exports = nextConfig
